


import { FormGroup, ValidationErrors, ValidatorFn } from '@angular/forms';

export const validando = (control: FormGroup): ValidationErrors | null => {



    console.log(control)

  return null;
};